doctype html
html(lang='en')
  head
    include ../includes/head.pug
  body(class=nconf.get('html:darkMode') ? 'theme-dark' : undefined)
    include ../includes/header.pug
    block content
    include ../includes/footer.pug
